\ProvidesPackage{../../templates/preamble}

\usepackage[english,russian]{babel}
\usepackage{graphicx}
\usepackage{tabularx}

\usepackage{fontspec}
\setmainfont{Times New Roman}

% Margins
\usepackage[
    top=10mm,
    bottom=10mm,
    left=10mm,
    right=10mm
]{geometry}

\usepackage{setspace}
\setlength{\parindent}{0pt}  % Paragraph indent
\setstretch{1.2}  % Line height

\usepackage{titlesec}
\titleformat{\section}[hang]{\normalfont\bfseries\centering\arraybackslash}{\thesection}{}{}
\titlespacing{\section}{0pt}{0pt}{0pt}

\usepackage{caption}
\usepackage{subcaption}
\DeclareCaptionLabelSeparator{emdash}{ --- }
\captionsetup[figure]{name={Рисунок},labelsep=emdash}

\pagenumbering{gobble}

\usepackage{floatflt}

\newcommand{\teacher}{Дедык А. И.}
\newcommand{\tteacher}{Дедык Антонина Ивановна}
\newcommand{\yyear}{2025}